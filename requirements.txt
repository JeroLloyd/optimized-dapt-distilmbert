torch>=2.0.0 --index-url https://download.pytorch.org/whl/cu118
transformers>=4.30.0
datasets>=2.14.0
accelerate>=0.21.0
scikit-learn>=1.3.0
pandas>=2.0.0
optimum>=1.12.0
onnx>=1.14.0
onnxruntime-gpu>=1.15.0
graphviz>=0.20.1  # Required for rendering pipeline flowchart (also install system Graphviz on Windows)
matplotlib>=3.6.0
networkx>=3.1

# On Windows, install Graphviz system binaries (add to PATH):
# - Using Chocolatey: choco install graphviz -y
# - Using winget: winget install --id Graphviz.Graphviz -e --source winget

py -3.10 -m venv venv
.\venv\Scripts\Activate.ps1